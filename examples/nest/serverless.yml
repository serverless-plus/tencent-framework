app: appDemo
stage: dev

component: framework
name: nestDemo

inputs:
  framework: nest
  src:
    src: ./
    exclude:
      - .env
  faas:
    name: ${name}
  apigw:
    protocols:
      - http
      - https
