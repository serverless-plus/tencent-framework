app: appDemo
stage: dev

component: framework@dev
name: expressDemo

inputs:
  framework: express
  src:
    src: ./
    exclude:
      - .env
  faas:
    name: framework-demo
  apigw:
    protocols:
      - http
      - https
