app: appDemo
stage: dev

component: framework@dev
name: eggDemo

inputs:
  framework: thinkphp
  src:
    src: ./
    exclude:
      - .env
  faas:
    name: framework-demo
  apigw:
    protocols:
      - http
      - https
